<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="wordbook">

	<!-- 쿼리문 작성 -->
	<select id="selectWordbookAllList" resultType="com.javaex.vo.WordbookVo" parameterType="java.lang.String">
	
		select   u.userno,
					u.nickname,
					d.directoryno,
					w.wordbookno,
					w.wordbookname,
					w.wordbookregdate,
					w.wordbookaccess,
					w.wordbookmaker
		from 	wordbook w,directory d,users u
		where 	u.id=#{URLId}
		and 		u.userno = d.userno
		and 		d.directoryno = w.directoryno
	
	</select>

	
<!-- 
	<insert id="insertGB" parameterType="com.javaex.vo.GuestBookVo">
		<![CDATA[
			insert into guestbook
			values (guestbook_seq.nextval,
			#{name},
			#{content},
			#{password},
			sysdate)
		]]>
	</insert>
-->
</mapper>
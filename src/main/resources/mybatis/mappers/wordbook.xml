<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="wordbook">

	<!-- 쿼리문 작성 -->

	<select id="selectNameDataTypeList" resultType="com.javaex.vo.WordbookVo">
		<![CDATA[
		
			select wordbook.wordbookname,
wordbook.wordbooktype 
from wordbook,directory,users 
where users.userno=directory.userno 
and directory.directoryno=wordbook.directoryno 
and users.id = 'seo'

		]]>
	</select>
<!-- 
	<insert id="insertGB" parameterType="com.javaex.vo.GuestBookVo">
		<![CDATA[
			insert into guestbook
			values (guestbook_seq.nextval,
			#{name},
			#{content},
			#{password},
			sysdate)
		]]>
	</insert>
-->
</mapper>
<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="flashcard">

	<!-- 쿼리문 작성 -->

	<select id="selectFlashcardWordList" resultType="com.javaex.vo.FlashcardVo">
		<![CDATA[
			select e.wordno, e.wordname
				from word_eng e, wordbook w
				where e.wordbookno = w.wordbookno
				and w.wordbookno = #{wordbookno}
		]]>
	</select>
	
	<select id="selectFlashcardMeanList" resultType="com.javaex.vo.FlashcardVo">
		<![CDATA[
			select k.meanno, k.meanname
				from word_eng e, mean_kor k, wordbook w
				where e.wordno = k.wordno
				and w.wordbookno = e.wordbookno
				and w.wordbookno = #{wordbookno}
		]]>
	</select>
</mapper>
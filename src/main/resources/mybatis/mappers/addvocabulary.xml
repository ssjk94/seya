<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="addvocabulary">

	<select id="select" resultType="com.javaex.vo.WordbookVo" parameterType="com.javaex.vo.URLPathVo">
		select directoryno, directoryname
			from 
			(select d.directoryname,
			     d.directoryno
			from	directory d, users u
			where u.id = #{URLId}
			and u.userno = d.userno)
		ORDER BY DIRECTORYNO ASC
	</select>

</mapper>